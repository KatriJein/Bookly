import { Header } from '../../header';
import { MainBanner } from '../banner';

export function MainPage() {
    return (
         <>
            <Header />
            <MainBanner />
        </>
    );
}